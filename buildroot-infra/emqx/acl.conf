%% EMQX ACL 规则 - 支持变量替换
%% %u = username, %c = clientid

%% 允许设备访问自己的 topic
{allow, all, publish, ["twin/%u/reported", "twin/%u/cmd"]}.
{allow, all, subscribe, ["twin/%u/desired", "twin/%u/ack"]}.

%% 默认拒绝
{deny, all}.