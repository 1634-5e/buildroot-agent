# Grafana Dashboard 自动配置
apiVersion: 1

providers:
  - name: 'Buildroot Dashboards'
    orgId: 1
    folder: 'Buildroot'
    folderUid: 'buildroot'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards