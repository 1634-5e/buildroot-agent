# Buildroot Agent 配置文件示例
# 复制此文件到 /etc/agent/agent.conf 并修改

# 服务器地址 (WebSocket)
# 格式: wss://host:port/path 或 ws://host:port/path
server_url = "wss://your-server.com/agent"

# 设备ID (唯一标识)
# 留空则自动生成 (基于MAC地址或machine-id)
device_id = ""

# 认证Token
# 用于服务端验证设备身份
auth_token = "your-auth-token-here"

# 心跳间隔 (秒)
# 定期发送心跳保持连接
heartbeat_interval = 30

# 重连间隔 (秒)
# 连接断开后的重连等待时间
reconnect_interval = 5

# 状态上报间隔 (秒)
# 系统状态信息上报频率
status_interval = 60

# 日志目录
# Agent监控的日志目录
log_path = "/var/log"

# 脚本存放目录
# 云端下发的脚本保存位置
script_path = "/tmp/agent_scripts"

# 是否启用PTY (远程Shell)
# 允许云端创建交互式Shell会话
enable_pty = true

# 是否启用脚本执行
# 允许云端下发并执行脚本
enable_script = true

# 日志级别 (debug, info, warn, error)
log_level = info
