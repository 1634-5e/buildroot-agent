# Buildroot Agent Configuration
# 
# 使用说明：
# 1. 复制此文件为 agent.conf: cp agent.conf.example agent.conf
# 2. 根据实际情况修改配置项
# 3. 运行 agent: ./buildroot-agent -c ./agent.conf
# 
# 此文件由程序自动生成，请勿手动编辑默认值
# 修改默认值请编辑 include/agent.h 中的 DEFAULT_* 宏定义

# ========================================
# 基础配置
# ========================================

# 服务器地址 (host:port)
server_addr = "127.0.0.1:8766"

# 设备ID (唯一标识，留空则自动生成)
device_id = ""

# Agent版本
version = "1.0.0"

# Token（已废弃，保留用于向后兼容）
# auth_token = ""

# ========================================
# 连接配置
# ========================================

# 心跳间隔 (秒)
heartbeat_interval = 30

# 重连间隔 (秒)
reconnect_interval = 5

# 状态上报间隔 (秒)
status_interval = 60

# ========================================
# 路径配置
# ========================================

# 日志目录
log_path = "./log"

# 脚本存放目录
script_path = "./scripts"

# ========================================
# 功能开关
# ========================================

# 是否启用PTY (远程Shell)
enable_pty = true

# 是否启用脚本执行
enable_script = true

# 日志级别 (debug, info, warn, error)
log_level = info

# ========================================
# SSL配置
# ========================================

# 是否启用SSL加密
use_ssl = false

# CA证书路径 (可选，留空使用系统证书)
ca_path = ""

# ========================================
# 自动更新配置
# ========================================

# 是否启用自动更新
enable_auto_update = false

# 更新检查间隔 (秒)
update_check_interval = 86400

# 更新渠道 (stable/beta/dev)
update_channel = "stable"

# 更新前是否需要确认
update_require_confirm = true

# 临时文件路径
update_temp_path = "./tmp"

# 备份路径
update_backup_path = "./backup"

# 失败是否自动回滚
update_rollback_on_fail = true

# 回滚超时 (秒)
update_rollback_timeout = 300

# 是否校验文件校验和
update_verify_checksum = true

# 更新CA证书路径 (可选，留空使用系统证书)
update_ca_cert_path = ""
