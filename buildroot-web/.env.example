# WebSocket Server Configuration
# Leave empty to auto-detect based on current page protocol and hostname
VITE_DEFAULT_WS_URL=

# Default max reconnection attempts (1-50)
VITE_DEFAULT_MAX_RECONNECT=10

# Default refresh interval in seconds (1-60)
VITE_DEFAULT_REFRESH_INTERVAL=5

# Auto-select first device on connection
VITE_DEFAULT_AUTO_SELECT=true

# Application Settings
VITE_APP_TITLE="Buildroot Agent 控制台"
VITE_APP_VERSION="2.1.0"
